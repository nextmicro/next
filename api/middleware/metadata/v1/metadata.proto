syntax = "proto3";

package next.middleware.metadata.v1;

option go_package = "github.com/nextmicro/next/api/middleware/metadata/v1";

// metadata middleware config.
message Metadata {
  repeated string Prefix = 1; // metadata prefix
}
